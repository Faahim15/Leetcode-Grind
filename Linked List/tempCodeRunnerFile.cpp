Node *dummy = new Node(0);
    // dummy->next = head;
    // Node *cur = head;
    // Node *prev = dummy;

    // while (cur->next)
    // {
    //     if (cur->val >= x && cur->next->val < x)
    //     {
    //         Node *temp = prev->next;
    //         prev->next = cur->next;
    //         cur->next = cur->next->next;
    //         cur->next->next = temp;
    //     }
    //     prev = prev->next;
    //     cur = cur->next;
    // }